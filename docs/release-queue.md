# Очередь релиза

Накопитель готовой, но ещё не опубликованной работы. Владелец ведёт несколько
диалогов параллельно, каждый доводит свою фичу до готовности и **останавливается
перед публикацией**; потом всё накопленное уезжает в прод **одним мерджем = одним
билдом Vercel**.

Правила работы с этим файлом — в `CLAUDE.md`, раздел "Очередь релиза". Коротко:

- сессия дописывает СВОЮ запись в "Ждут публикации" сверху, на своей ветке,
  и на этом заканчивает — в `oodobu` ничего не пушит;
- публикующая сессия собирает все перечисленные ветки в одну релизную,
  прогоняет сборку и открывает ОДИН PR в `oodobu`;
- после публикации записи переезжают в "Опубликовано" (внизу), не удаляются.

Конфликт в этом файле при мердже (две сессии дописали сверху) разрешается как
в журнале — **сохранять обе записи**, не выбирая сторону.

---

## Ждут публикации

Формат записи:

```
### <ветка>
- **Что:** одна строка, что меняется с точки зрения владельца
- **Проверено:** чем (сборка, мок-тест, скриншоты, ручной прогон)
- **Миграции SQL:** применены / нет; какие именно
- **Риск при задержке:** есть/нет и какой
```

### claude/wonderful-einstein-vdg945 — ОТЛОЖЕНА ОСОЗНАННО
- **Что:** очередь повторной отправки одиночных писем (отказ Resend больше не
  теряет письмо): новая таблица `outgoing_email_jobs`, колонки `send_status`/
  `send_error` у `supplier_offer_emails` и `purchase_emails`, Edge Function
  `process-outgoing-emails`.
- **Почему не в релизе 2026-09-12:** в шапке её миграции
  (`supabase/migrations/20260912-outgoing-email-queue.sql`) прямым текстом:
  «эта миграция ещё НЕ применена к проду (владелец попросил не выкатывать до
  обновления тарифа)». Проверено запросом к базе — таблицы
  `outgoing_email_jobs` там действительно нет, то есть код бы поехал в прод
  и обращался к несуществующей таблице.
- **Что нужно для публикации (все три шага, по порядку):**
  1. обновить тариф Resend (ради этого и откладывалось);
  2. применить миграцию, затем задеплоить Edge Function
     `process-outgoing-emails`;
  3. только ПОСЛЕ деплоя функции раскомментировать и выполнить блок
     `cron.schedule` в конце той же миграции — иначе крон каждую минуту будет
     стучаться в несуществующий эндпоинт.
- **Риск при задержке:** нет, ровно наоборот — выкатывать раньше нельзя.

---

## Не в очереди (разобрать отдельно)

### claude/modular-platform-client-sales-r6fgw4
31 коммит от 2026-09-09, 38 файлов, ~7.5к строк. Верхний коммит — "Add plan
for the client-license prototype (separate repo + Supabase project)", то есть
похоже на заготовку под ОТДЕЛЬНЫЙ репозиторий, а не под этот прод. В очередь
намеренно не включена: слишком большой и непонятный объём, чтобы вливать
заодно. Разобрать отдельной сессией — выяснить у владельца, живая это работа
или брошенная.

---

## Опубликовано

### Релиз 2026-09-12 (третий, по команде «публикуй»)

Одна ветка в очереди, поэтому отдельная релизная ветка не собиралась:
`claude/elegant-davinci-rpxbbp` = `oodobu` + один коммит, конфликтов нет,
опубликована своим PR напрямую в `oodobu` (один PR = один прод-билд Vercel).
`npm run build:app` на ней чистый. SQL-миграций в релизе нет.

Что было и что НЕ вошло на момент сборки:

- в «Ждут публикации» кроме неё оставалась только
  `claude/wonderful-einstein-vdg945` с пометкой «ОТЛОЖЕНА ОСОЗНАННО» — её
  запись оставлена как есть, не публиковалась;
- `claude/release-2026-09-12-3` — уже неактуальная ветка: по коду совпадает с
  `oodobu` до буквы (её содержимое доехало в прод через PR #52/#54),
  расходятся только журнал и этот файл. Мерджить нечего, можно удалять;
- `claude/zen-johnson-i7vjcg` (11:24 того же дня) — один новый файл
  `api/_belarus.js`: валидатор УНП и клиент ГРП МНС, сам коммит говорит
  «пока не подключён». Записи в очереди у ветки нет, значит другая сессия её
  готовой не объявляла — в релиз не бралась.

### claude/elegant-davinci-rpxbbp
- **Что:** массовая рассылка по профильной категории больше не подмешивает
  универсальных поставщиков. Владелец: «когда я отправляю рассылку узким
  поставщикам (например, краска), мне в общем списке в рассылке не нужны
  универсальные поставщики вообще... универсальную ведомость я буду рассылать
  отдельно». Откат поведения из PR #52 (оно уже в проде): убраны галочка
  «Добавить Универсальные поставщики», селект мастер-ведомости для них, метка
  «· универсальный» и второе задание очереди — в очередь встаёт одно задание
  с ведомостью категории. Универсальным теперь пишется отдельной рассылкой:
  категория «Универсальные поставщики» в селекте модалки + мастер-ведомость
  выбирается в пикере ведомости (он их и так показывает).
- **Проверено:** `npm run build:app` чистый; мок-тест BulkSendModal со
  скриншотами — «Краска» показывает только профильных, «Универсальные
  поставщики» только универсальных.
- **Миграции SQL:** нет, правка целиком во фронте (`BulkSendModal.tsx`,
  один проп в `Suppliers.tsx`).
- **Риск при задержке:** низкий, но неприятный для владельца: до публикации в
  проде остаётся нежелательное поведение — универсальные видны и по умолчанию
  отмечены в рассылке по любой узкой категории, так что случайное «Поставить в
  очередь» отправит им ведомость краски. Обходится вручную: снять галочку
  «Добавить Универсальные поставщики» перед отправкой.
- **Конфликты при сборке:** только журнал (`docs/session-journal.md`) и этот
  файл — сохранять обе записи. Правки в `BulkSendModal.tsx` пересекутся с
  любой другой веткой, которая трогает массовую рассылку; смысл правки —
  удаление универсальной группы, при конфликте выигрывает удаление.


### Релиз 2026-09-12 (второй, по команде «собирай всю доступную очередь»)

Собран в `claude/release-2026-09-12-2` от `oodobu`, пять веток одним PR = один
прод-билд Vercel. `npx tsc -b`, `npx vitest run` (88 тестов) и `npm run build:app`
чистые на собранной ветке; дополнительно прогнан сам пререндер в быстром режиме —
284 пути скопированы с прода с перенацеливанием ассетов, все 285 итоговых
снапшотов проверены: ни одной ссылки на несуществующий чанк, во всех есть входной
чанк текущей сборки.

- `claude/wizardly-pascal-r8lt3l` — мастер-ведомость: сводная ведомость на смету,
  собирается из остальных сама и обновляется при любом их изменении.
- `claude/sweet-shannon-9kwco6` — универсальный поставщик живёт одной карточкой
  (предупреждение о дубле, объединение карточек с переносом переписки/КП/файлов,
  баннер для разовой чистки), универсальные поставщики в массовой рассылке,
  страна в сравнении карточек.
- `claude/optimistic-newton-y97o9x` — счета из писем записываются в базу сами, без
  ручного «Подтвердить»; плюс три починки распознавания (.xlsx-счета, выбор
  правильного вложения вместо картинки из подписи, вложения без имени). Бэкофилл
  по реальной базе уже прогнан ДО публикации (5 счетов).
- `claude/vigilant-thompson-l67u4o` + `claude/bold-rubin-ptv4ew` — **две сессии
  независимо чинили одно и то же**: прод-билд по 10 минут на каждом пуше (быстрый
  режим пререндера отвергал копию с прода из-за несовпадения хэша входного чанка,
  а хэши пересчитываются каскадом от любой правки, даже чисто админской). При
  сборке релиза взяты непересекающиеся части:
  - от `vigilant-thompson` — перенацеливание ассетов снапшота по стему имени чанка,
    повторы (3 попытки) на select к Supabase в `prerender.mjs`/`generate-sitemap.mjs`
    (разовый 504 free-tier дважды за сутки ронял прод-деплой) и правило в `CLAUDE.md`
    про ожидание деплоя;
  - от `bold-rubin` — отпечаток публичного кода (`scripts/public-build-id.mjs`, новый
    шаг в `build:app` → `dist/public-build-id.txt`): полный рендер включается сам,
    когда правился код маркетинговых страниц, а правки админки идут быстрым путём.
  Дубль-реализация из `bold-rubin` (`scripts/prerender-assets.mjs` и её тест) при
  сборке удалена — механизм остался один.
- `claude/wizardly-maxwell-hvcere` — только запись в очереди про прямой пуш текста
  массовой рассылки (сам коммит `9f6ddd1` уже был в `oodobu`).

**Что учесть после публикации:** первый прод-билд снова будет полным — на проде
ещё нет `/public-build-id.txt`, отпечаток сверять не с чем. Быстрые билды
начнутся со следующего пуша, не трогающего публичные страницы.

**Догон по `claude/sweet-shannon-9kwco6` (влит отдельно, сразу после релиза):**
релиз собрали по состоянию ветки на коммит `f111a9a`, а в ней к тому моменту
были ещё три: мастер-ведомость универсальным поставщикам в массовой рассылке
(рассылка встаёт в очередь двумя заданиями — профильным ведомость категории,
универсальным мастер-ведомость), схлопнутый в один `useMemo`
`materialLedgersWithMasters` вместо двух копий и записи в журнале/очереди.
Влиты в `oodobu` отдельным PR по команде владельца («билд прошёл, заливай
остальное»); дубликатов после мерджа нет — проверено по объявлениям
`materialLedgersWithMasters`/`masterLedgerForEstimate`/`handleLedgersChange`
и по импорту `lib/masterLedger`.

### claude/wonderful-einstein-vdg945 — опубликована раньше, вне очереди

Запись в «Ждут публикации» устарела: работа уехала в прод отдельным PR #49
(«очередь для одиночных писем»). Проверено 2026-09-12 запросом к базе — таблица
`outgoing_email_jobs` существует, крон `process-outgoing-emails` заведён, Edge
Function и миграция лежат в `oodobu`. Больше ничего делать не нужно.

### Прямой пуш 2026-09-12 (вне очереди, по просьбе владельца)

- `claude/wizardly-maxwell-hvcere` → коммит `9f6ddd1` в `oodobu`. Новый текст
  дефолтного письма массовой рассылки поставщикам (`defaultBulkBody()` в
  `src/components/suppliers/BulkSendModal.tsx`): «Планируем закупку материала
  согласно ведомости, прикрепленной к письму» + «Карточку организации и адрес
  доставки прикрепил к письму». Только текст, логика рассылки и подстановка
  `{компания}`/`{контакт}` не трогались. `npm run build:app` чистый,
  SQL-миграций нет. Пушился напрямую по команде «пушь сразу в прод»; владелец
  сразу после этого попросил впредь ставить в очередь — запись здесь, чтобы
  коммит не потерялся при сборке следующего релиза.

### Релиз 2026-09-12 (первый через очередь)

Собран в `claude/release-2026-09-12` от `oodobu`, семь веток одним PR = один
прод-билд Vercel. `npx tsc -b` и `npm run build:app` чистые на собранной ветке.

- `claude/peaceful-feynman-98kvhd` — транзакции: вкладки периодов (этот месяц /
  прошлый / год / произвольный), дата новой транзакции по умолчанию = сегодня,
  сортировка по дате после добавления и правки, «Альфа» убрана из источников,
  подкатегория «ИИ» у Claude/ProxyAPI. Плюс сама очередь релиза.
  SQL-миграции применены заранее (правка данных, не схемы).
- `claude/great-bardeen-0xvlmt` — метрики обновляются раз в минуту без
  перезагрузки страницы.
- `claude/epic-archimedes-k7wmyk` — задачи: сотрудник видит только свои,
  владелец весь список. Схему не трогает, фильтрация на клиенте
  (`isTaskVisible`) — миграция не нужна, проверено при сборке релиза.
- `claude/epic-allen-lwowmr` — уникальное превью ссылки под каждую страницу
  (+ шаги сборки `generate-og-cards` и `generate-admin-shells`).
- `claude/happy-planck-5tnxyl` — убрана кнопка «Найти в сети» с карточки
  категории поставщиков.
- `claude/dreamy-faraday-jc3bc3` — веб-поиск поставщиков: логирование раундов,
  пропуск второго раунда для узких рынков (`MIN_ROUND1_FOR_SECOND_ROUND`).
- `claude/sharp-hawking-f1n70s` — бейдж уведомления в `ToggleGroup` не
  обрезается по правому краю.

**Конфликты, разобранные при сборке** (пригодится на будущее):
- `ToggleGroup.tsx` — один и тот же баг чинили два диалога по-разному. Взяты
  обе половины: `-mr-2` из ветки + `z-10` у бейджа из `oodobu`.
- `package.json` — обе стороны дописали свои шаги в `build`/`build:app`.
  Объединены все три: `notify-indexnow` (из oodobu), `generate-og-cards` и
  `generate-admin-shells` (из ветки). Проверено — сборка прогоняет все.
- `scripts/process-supplier-web-search-jobs.mjs` — ветка писала по старому
  имени `country`, в `oodobu` колонка уже переименована в `region`. Взята
  сигнатура из `oodobu`, логирование и порог второго раунда — из ветки.
- `CLAUDE.md` у `dreamy-faraday` — ветка старая, дописывала журнал ещё в
  `CLAUDE.md`, откуда он с тех пор переехал. Взята сторона `oodobu`, а
  уникальная запись ветки (180 строк про логи ProxyAPI) перенесена в
  `docs/session-journal.md`, а не потеряна.
