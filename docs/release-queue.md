# Очередь релиза

Накопитель готовой, но ещё не опубликованной работы. Владелец ведёт несколько
диалогов параллельно, каждый доводит свою фичу до готовности и **останавливается
перед публикацией**; потом всё накопленное уезжает в прод **одним мерджем = одним
билдом Vercel**.

Правила работы с этим файлом — в `CLAUDE.md`, раздел "Очередь релиза". Коротко:

- сессия дописывает СВОЮ запись в "Ждут публикации" сверху, на своей ветке,
  и на этом заканчивает — в `oodobu` ничего не пушит;
- публикующая сессия собирает все перечисленные ветки в одну релизную,
  прогоняет сборку и открывает ОДИН PR в `oodobu`;
- после публикации записи переезжают в "Опубликовано" (внизу), не удаляются.

Конфликт в этом файле при мердже (две сессии дописали сверху) разрешается как
в журнале — **сохранять обе записи**, не выбирая сторону.

---

## Ждут публикации

Формат записи:

```
### <ветка>
- **Что:** одна строка, что меняется с точки зрения владельца
- **Проверено:** чем (сборка, мок-тест, скриншоты, ручной прогон)
- **Миграции SQL:** применены / нет; какие именно
- **Риск при задержке:** есть/нет и какой
```

### claude/peaceful-feynman-98kvhd
- **Что:** транзакции — вкладки периодов (этот месяц / прошлый / год /
  произвольный), дата новой транзакции по умолчанию = сегодня, сортировка по
  дате после добавления и правки, "Альфа" убрана из источников, подкатегория
  "ИИ" у Claude/ProxyAPI. Плюс сама эта очередь релиза (этот файл + раздел в
  `CLAUDE.md`).
- **Проверено:** `npm run build:app` чистый; мок-тест с Playwright (вкладки,
  произвольный период, дата в модалке, порядок строк после вставки задним
  числом, список источников без Альфы).
- **Миграции SQL:** ПРИМЕНЕНЫ 2026-09-12 — `paid_from: 'Альфа' → 'Личные
  деньги'` (2 строки), `subcategory = 'ИИ'` для Claude/ProxyAPI (6 строк).
  Обе — правка данных, не схемы.
- **Риск при задержке:** нет. Живой прод продолжает показывать эти
  транзакции корректно, просто без новых вкладок.

### claude/wonderful-einstein-vdg945
- **Что:** очередь для одиночных писем — отказ Resend больше не теряет письмо.
- **Проверено:** запись восстановлена по коммиту при заведении очереди
  (2026-09-12), автор ветки подробностей не оставил — **перед публикацией
  подтвердить у владельца, что фича закончена**.
- **Миграции SQL:** неизвестно — проверить перед публикацией.
- **Риск при задержке:** неизвестно.

### claude/happy-planck-5tnxyl
- **Что:** убрана кнопка "Найти в сети" с карточки категории поставщиков.
- **Проверено:** запись восстановлена по коммиту (см. оговорку выше).
- **Миграции SQL:** неизвестно — проверить перед публикацией.
- **Риск при задержке:** неизвестно.

### claude/epic-archimedes-k7wmyk
- **Что:** задачи — сотрудник видит только свои, владелец весь список.
- **Проверено:** запись восстановлена по коммиту (см. оговорку выше).
- **Миграции SQL:** неизвестно — **важно проверить**: разграничение доступа
  обычно идёт вместе с RLS-политиками, а они применяются в живую базу сразу.
- **Риск при задержке:** возможен — если RLS уже ужесточён в базе, а код ещё
  не опубликован, живой прод может не увидеть части задач.

### claude/epic-allen-lwowmr
- **Что:** уникальное превью ссылки под каждую страницу.
- **Проверено:** запись восстановлена по коммиту (см. оговорку выше).
- **Миграции SQL:** неизвестно — проверить перед публикацией.
- **Риск при задержке:** нет (SEO-превью, чем раньше опубликовано, тем лучше,
  но простой не ломает ничего).

### claude/dreamy-faraday-jc3bc3
- **Что:** веб-поиск поставщиков — логирование usage, `MAX_SEARCHES` 30→20,
  пропуск второго раунда для узких рынков.
- **Проверено:** запись восстановлена по коммиту (см. оговорку выше).
- **Миграции SQL:** неизвестно — проверить перед публикацией.
- **Риск при задержке:** нет, но ветка трогает `scripts/process-supplier-web-
  search-jobs.mjs` и `CLAUDE.md` — при сборке релиза будут конфликты, решать
  руками.
- **Конфликты при сборке:** `CLAUDE.md`, `scripts/process-supplier-web-search-jobs.mjs`.

### claude/sharp-hawking-f1n70s
- **Что:** не обрезать бейдж уведомления в `ToggleGroup` (правый край).
- **Проверено:** запись восстановлена по коммиту (см. оговорку выше).
- **Миграции SQL:** нет (чистый CSS-фикс).
- **Риск при задержке:** нет.
- **Конфликты при сборке:** `src/components/ui/ToggleGroup.tsx` — ДУБЛИРУЮЩИЙ
  фикс: тот же баг уже починен в `oodobu` другим диалогом, но по-своему.
  Правильное разрешение (проверено 2026-09-12): взять `-mr-2` из этой ветки
  (правый край действительно обрезался) И сохранить `z-10` у бейджа из
  `oodobu` (иначе соседняя кнопка перекрывает кружок). Не брать сторону
  целиком — потеряется половина фикса.

---

## Не в очереди (разобрать отдельно)

### claude/modular-platform-client-sales-r6fgw4
31 коммит от 2026-09-09, 38 файлов, ~7.5к строк. Верхний коммит — "Add plan
for the client-license prototype (separate repo + Supabase project)", то есть
похоже на заготовку под ОТДЕЛЬНЫЙ репозиторий, а не под этот прод. В очередь
намеренно не включена: слишком большой и непонятный объём, чтобы вливать
заодно. Разобрать отдельной сессией — выяснить у владельца, живая это работа
или брошенная.

---

## Опубликовано

(пока пусто — первая публикация через очередь ещё не состоялась)
