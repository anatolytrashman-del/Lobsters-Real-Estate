# План оптимизации PageSpeed — `/minsk/minsk-mir` (и остальные публичные страницы)

Рабочий документ для Claude (тот же принцип, что `SEO_PLAN.md`): при любой
работе над скоростью сайта — СНАЧАЛА открыть этот файл, брать этапы отсюда,
отмечать сделанное в чекбоксах и журнале внизу. В `CLAUDE.md` — только
короткая запись-ссылка сюда, детали не дублировать.

Составлен 2026-09-06 по двум файлам от владельца (webarchive + PDF — это один
и тот же отчёт PageSpeed Insights, мобильный, 17:46 GMT+3, Lighthouse 13.4.1,
Moto G Power, Slow 4G). Все цифры ниже — из него, плюс собственная проверка
кода и прода (curl) в той же сессии.

## 0. Что показал отчёт (мобильный, `https://redevelopment.pro/minsk/minsk-mir`)

| Метрика | Значение | Оценка |
|---|---|---|
| **Performance** | **47** | красная зона |
| First Contentful Paint | 5,1 с | красная |
| Largest Contentful Paint | 7,6 с | красная |
| Total Blocking Time | 480 мс | жёлтая |
| Speed Index | 7,7 с | красная |
| Cumulative Layout Shift | 0 | зелёная |
| Accessibility | 86 | контраст |
| Best Practices | 96 | — |
| SEO | 100 | — |

Данных CrUX (реальные пользователи) нет — трафика пока мало, оценка только лабораторная.

Ключевые находки отчёта (в порядке вклада в оценку):

1. **Первый документ — 3,41 КиБ (сжатых).** Это пустой SPA-шелл, не пререндер.
   Контент появляется только после загрузки/выполнения JS → отсюда FCP 5,1 с
   и SI 7,7 с. LCP-картинка (`/images/district/hero-5.jpg`) тоже открывается
   только из JS: «Resource load delay 540 мс + Element render delay 640 мс».
2. **Яндекс.Карты (`yastatic.net/.../release/full.js`, 689 КиБ)** — 2 174 мс
   CPU (из 4,1 с всей main-thread работы), 6 из 10 длинных задач, 456 КиБ
   неиспользуемого JS, forced reflow 147 мс, 370 дочерних узлов в DOM.
   Грузятся сразу при открытии страницы, хотя обе карты далеко внизу.
3. **Шрифты — 4 × ~109 КиБ TTF** (Regular/Medium/SemiBold/ExtraBold) на
   критическом пути (Network dependency tree: max critical path latency 655 мс —
   ровно шрифты). Исходники в `public/fonts/` — по 260 КБ каждый, TTF без
   сабсета.
4. **Яндекс.Метрика (`tag.js`, 87 КиБ)** — 796 мс CPU, 2 длинные задачи в
   окне загрузки (2,6–2,8 с), 42 КиБ неиспользуемого кода. Инициализируется
   инлайн-скриптом в `<head>` с `webvisor: true, clickmap: true`.
5. **Картинки — «Est savings 151 КиБ»**: логотипы PNG в 3–4 раза больше
   отображаемого размера (Dana Holdings 585×176 → 209×63; Happy Planet
   200×198 → 85×84; Минск Мир 171×200 → 54×63), hero-фото — JPEG без WebP,
   `/icons/minsk-metro-line3.png` — 640×302 / 72 КиБ для иконки высотой 16 px.
   У всех `<img>` нет `width`/`height`.
6. **Данные первичного рынка — 7 запросов к Supabase ×~40 КиБ = 253 КиБ**
   (`primary_market_offers?select=*&offset=0..6000&limit=1000`) — вся таблица
   (6 158 строк) целиком, последовательно, на каждое открытие страницы, ради
   5 строк сводной таблицы.
7. **Главный JS-чанк — 167 КиБ (сжатых), 71 КиБ не используется на этой
   странице**; отдельный `supabase-*.js` 53 КиБ, из них 43 КиБ не используется.
8. **Кэш**: Lighthouse пожаловался только на 3rd-party (Метрика/Карты), но
   по факту (curl -I) ВСЯ статика прода отдаётся с
   `cache-control: public, max-age=0, must-revalidate` — включая хэшированные
   `/assets/*.js`, шрифты и картинки. Для первого захода не важно, для
   повторных — каждый раз ревалидация всех файлов.
9. **Accessibility 86** — только контраст: `text-primary` (#e4152b) на светлом
   фоне («RED» в логотипе, ссылка «деловой центр Red One»), `text-ink-faint`
   (#9a9ba3) на `bg-bg`/белом (подписи «Категория бизнеса», «Квартал»,
   «Меньше», uppercase-заголовки колонок таблиц, мелкие подписи `text-xs`),
   зелёный бейдж «Обновлено: …» (`text-success` #1aa053 на `bg-success-bg`).

## 1. Что нашлось сверх отчёта при проверке прода (2 реальных бага, не «оптимизация»)

- [x] **Б1. Пререндер `/minsk/minsk-mir` не работает на проде** — было:
  `curl https://redevelopment.pro/minsk/minsk-mir` → 8 477 байт, 0 `<img>`,
  нет `<h1>` (пустой `dist/index.html`-шелл из общего rewrite, не снапшот).
  **ИСПРАВЛЕНО (Э0), точный диагноз по Build Logs, которые прислал владелец**:
  94 из ~194 путей проваливались с `page.content`/`page.waitForSelector:
  Target page, context or browser has been closed`, включая сам
  `/minsk/minsk-mir`. Причина — не «нестабильность вообще», а конкретный флаг
  `@sparticuz/chromium` на Vercel: Chromium запускается с `--single-process`
  (виден в самой команде запуска в логах) — один OS-процесс на браузер И ВСЕ
  его вкладки разом, без изоляции рендереров. Крах рендерера ОДНОЙ вкладки
  убивает процесс целиком — вместе с ним гибнут и остальные вкладки, открытые
  в этот момент ДРУГИМИ воркерами. Чем выше `WORKER_COUNT`, тем больше вкладок
  гарантированно теряется на каждом таком крахе — сам параллелизм не ускорял
  сборку, а систематически множил потери. См. Э0-2..Э0-4 для фикса.
- [x] **Б2. Таблица «Вторичный рынок» на проде пуста для посетителей** — в
  отчёте Lighthouse видно «Данные пока не собраны.» (Accessibility-секция,
  блок `#market`). Причина была: `fetchMarketOffers()`
  (`src/lib/marketOffersApi.ts:31`) делает `select('*')`, а после P0.2 аудита
  безопасности (2026-08-28) anon имел колоночный `GRANT SELECT` на
  `market_offers` — `select=*` при column-level privileges Postgres проваливает
  ВСЮ выборку 42501, а не тихо прячет непозволенные колонки (проверено curl'ом:
  `select=id` работал, `select=*` — нет). **ИСПРАВЛЕНО (Э5-1)** — см. там.

## 2. Этапы (в порядке выполнения — сверху максимальная отдача за минимум работы)

Оценки эффекта — ориентировочные, из структуры отчёта (сколько миллисекунд/КиБ
даёт каждый пункт), не обещание конкретного балла. Реалистичная цель после
Э0–Э4: **мобильный Performance 75–85** (сейчас 47). 90+ потребует ещё и Э6/Э7.

После КАЖДОГО этапа: `npx tsc -b && npm run build` (см. §4 про то, что
`prerender.mjs` в песочнице виснет — это известно), коммит, мердж в
`claude/redevelopment-platform-prototype-oodobu` (прод), и **повторный
PageSpeed владельцем на проде** — чтобы видеть вклад каждого этапа отдельно, а
не гадать, что сработало.

### Э0. Вернуть пререндер `/minsk/minsk-mir` (и остальных пропавших путей) — Б1

Самый большой рычаг: FCP/SI/LCP сразу упираются в HTML, а не в JS. Ожидаемо
FCP 5,1 с → ~1,5–2 с, SI 7,7 с → ~3 с, LCP 7,6 с → ~3–4 с (картинка hero
становится «discoverable in initial document» без JS).

- [x] Э0-1. **Диагностика по логам Vercel** — владелец прислал Build Logs.
  Каждый сбой — ровно `page.content`/`page.waitForSelector: Target page,
  context or browser has been closed`, ни разу «Timeout waiting for h1» без
  сопутствующего «closed» и ни разу «Загрузка… не исчезла» как единственная
  причина потери страницы. Значит дело именно в крахах браузера (см. Б1), не
  в медленной загрузке данных.
- [x] Э0-2. **Сделать пропуск пререндера заметным.** `scripts/prerender.mjs`:
  `criticalPaths = new Set([...landingPaths, ...STATIC_PATHS])` (лендинги
  объектов + все статические страницы, включая `/minsk/minsk-mir`), собирается
  `failedPaths` по ходу рендера; в конце `main()` — если среди провалившихся
  путей есть хоть один критичный, `process.exitCode = 1` со списком путей в
  логе (сборка на Vercel красная, прод остаётся на прошлой версии). Провал
  некритичного пути (карточка БЦ/хаб пересечения) — просто `console.warn` со
  списком, сборка продолжается.
- [x] Э0-3. **Стабилизировать пул.** `WORKER_COUNT` 4 → **1** (не 2 — при
  `--single-process` даже двух одновременных вкладок достаточно для
  каскадного краха, см. Б1; строго последовательный рендер — единственный
  вариант, где крах роняет максимум одну текущую страницу). Число попыток
  2 → 3, между попытками после краха — пауза `500мс × номер попытки` (даёт
  ОС время реально освободить ресурсы упавшего процесса перед повторным
  `launch()`). Таймауты `waitForSelector('h1')`/`waitForFunction('Загрузка…')`
  оставлены как были (20с/15с) — раз дело было не в них (см. Э0-1), увеличивать
  не потребовалось.
- [x] Э0-4. **Убрать из снапшота то, чего там быть не должно.** `loadYmaps()`
  (`lib/yandexMaps.ts`) при `location.search.includes('prerender=1')` теперь
  возвращает промис, который никогда не резолвится — компонент карты остаётся
  на «Загрузка карты…», сам API Яндекса (689 КиБ) в build-time рендер вообще
  не грузится.
- [ ] Э0-5. **Проверка на проде после деплоя** (обязательна, ждёт мерджа в
  прод и деплоя — из песочницы не проверить, headless-браузер здесь не
  достаёт до `*.supabase.co`, см. §4): `curl -sS
  https://redevelopment.pro/minsk/minsk-mir | wc -c` → должно быть ≥ 100 000
  байт; `grep -c '<h1'` → 1; `grep -c 'hero-1.webp'` → ≥ 1. То же для
  `/minsk/bcminsk` и одной карточки БЦ (`/minsk/bcminsk/titan`). Также
  проверить в самих Build Logs новой сборки — сколько путей пропущено
  (некритичных) и что критичных пропусков нет (иначе деплой должен был стать
  красным сам, но полезно свериться глазами).

Файлы: `scripts/prerender.mjs`, `src/lib/yandexMaps.ts`.

### Э1. Шрифты: TTF → WOFF2 с сабсетом, только используемые веса, preload

Сейчас: 6 файлов `@font-face` в `src/index.css` (Thin/Light/Regular/Medium/
SemiBold/ExtraBold), по 260 КБ TTF. `font-thin`/`font-extralight`/`font-light`
в `src/**/*.tsx` **не встречаются ни разу** (проверено grep: semibold 311,
medium 197, bold 157, extrabold 43, black 20, normal 2) → Thin и Light —
мёртвый груз (не грузятся, пока нет текста такого веса, но зачем держать).
Реально нужны 4 веса, они и тянутся: 4 × 109 КиБ = 436 КиБ на критическом пути.

- [x] Э1-1. `pyftsubset` (fonttools) прогнан на все 4 веса — 260 КБ TTF →
  ~24 КБ WOFF2 каждый (91%). Unicode-набор: кириллица+латиница+пунктуация+
  валюты+тире/кавычки. `tnum`/`pnum`/`lnum`/`onum`/`liga`/`locl`/`mark`/`mkmk`
  сохранены — проверено программно (`TTFont(...).get('GSUB')`, все теги на
  месте) И по реальному тексту страницы (regex по кириллице/цифрам/спецсимволам
  из самой страницы — все глифы покрыты). Thin/Light не пересобирались —
  удалены совсем (см. Э1-4).
- [x] Э1-2. `src/index.css` — `src: url(...woff2) format('woff2')` на всех 4
  `@font-face`. `size-adjust`/`ascent-override` НЕ добавлены — CLS и так 0,
  делать это профилактически без измеримой проблемы не стали.
  **Найден и исправлен реальный баг минификации при этой правке**: собственный
  комментарий над `@font-face`-блоком содержал путь-глоб с последовательностью
  «две звёздочки-слэш-звёздочка» — эта же последовательность (звёздочка
  вплотную перед слэшем) является закрывающей для CSS-комментария, поэтому
  `/* … */` обрывался раньше срока, и lightningcss (минификатор Tailwind v4)
  молча терял следующее за ним правило `@font-face` для Regular — весь текст
  сайта рендерился бы системным шрифтом вместо Montserrat, никакой ошибки
  сборки при этом не было. Пойман не на глаз, а прогоном `npx vite build` +
  `grep` по собранному CSS на все 4 `@font-face`/имени файлов — 3 из 4 (без
  Regular) до фикса, 4 из 4 после. Комментарий переписан без такой
  последовательности символов, отдельно оставлено явное предупреждение на
  будущее в самом файле.
- [x] Э1-3. `<link rel="preload" as="font" type="font/woff2" crossorigin>` в
  `index.html` — 3 веса (Regular/SemiBold/ExtraBold; Medium не preload —
  преимущественно текст ниже сгиба).
- [x] Э1-4. TTF-файлы (все 6, включая Thin/Light) удалены из
  `public/fonts/` физически, ссылок на `.ttf` в репозитории не осталось
  (проверено `grep -rl "\.ttf"` по `dist/` после сборки — пусто).

Эффект: 1,5 МБ шрифтов → 100 КБ. Файлы: `public/fonts/`, `src/index.css`,
`index.html`.

### Э2. Яндекс.Карты — грузить только при приближении к вьюпорту + меньший набор модулей

Обе карты внизу страницы (`DistrictMap` — самый низ, строка ~2355; `DistrictQuarterMap`
— середина, ~1696), но `loadYmaps()` вызывается в `useEffect` при монтировании
(`DistrictMap.tsx:49`, `DistrictQuarterMap.tsx:265`) → `full.js` 689 КиБ +
2,1 с CPU на старте, когда пользователь ещё смотрит hero.

- [x] Э2-1. `src/lib/useInView.ts` — новый общий хук (IntersectionObserver,
  один раз → `true`, потом disconnect). `DistrictMapCanvas` (`DistrictMap.tsx`)
  получил проп `deferUntilVisible` (default `true`): компактная карта на
  странице грузит `loadYmaps()` только при приближении к вьюпорту, а карта
  внутри полноэкранной модалки (`deferUntilVisible={false}`) — сразу, раз
  пользователь только что сам нажал «Открыть на весь экран», ждать нечего.
  `DistrictGuidePage.tsx` — те же два блока (`#quarter-map`/`#map`) обёрнуты
  в `useInView`, до появления в вьюпорте рендерится тот же плейсхолдер
  «Загрузка карты…», что был у `Suspense fallback`.
- [x] Э2-2. `DistrictQuarterMap` — рендер (`<Suspense><DistrictQuarterMap/>`)
  дополнительно гейтится тем же `useInView` в родителе, не только
  `lazy()`-импорт.
- [x] Э2-3. `DistrictMap` тоже переведён на `lazy()` (был статическим
  импортом) + `useInView` на рендер, как `DistrictQuarterMap`. Проверено
  сборкой: строка «Николы Теслы» (маркер `data/districtPlaces.ts`) пропала
  из главного чанка, переехала в отдельный лениво загружаемый чанк.
- [ ] Э2-4. **Не сделано, сознательно.** Параметр `load=` для API 2.1
  Яндекс.Карт нельзя проверить из этой песочницы (`api-maps.yandex.ru`
  заблокирован прокси для headless-браузера — подтверждено отдельным
  probe-скриптом в этой же сессии: запрос к `*.supabase.co` из headless
  Chromium зависает на уровне соединения, тот же класс блокировки
  документирован для Яндекс.Карт в CLAUDE.md). Ломающийся набор модулей на
  проде — риск полностью убить обе карты сайта ради экономии, которую
  нельзя предварительно измерить надёжно. После Э2-1..3 карты и так не
  грузятся, пока не долистали — отдача от урезания самого `full.js` намного
  меньше. Оставлено как есть; если понадобится — делать отдельным
  коммитом на проде с немедленной ручной проверкой обеих карт.
- [x] Э2-5. `?prerender=1` → `loadYmaps()` возвращает промис, который никогда
  не резолвится (см. Э0-4) — реализовано одним изменением на оба сценария.

Эффект: TBT 480 мс → ~150–200 мс, main-thread −2 с, «unused JS» −456 КиБ,
DOM −400 узлов на старте. Файлы: `src/lib/yandexMaps.ts`, `src/lib/useInView.ts`
(новый), `src/components/district/DistrictMap.tsx`, `DistrictQuarterMap.tsx`,
`src/pages/DistrictGuidePage.tsx`.

### Э3. Яндекс.Метрика — отложить загрузку, отключить лишнее

`index.html:70-81`: инлайн-инициализация в `<head>` с `webvisor: true,
clickmap: true` → 87 КиБ + 796 мс CPU в самом начале загрузки.

- [x] Э3-1. `tag.js` вставляется в DOM не сразу, а по `window` `load` +
  `setTimeout(…, 2000)` (`document.readyState === 'complete'` — сразу с той
  же задержкой, без ожидания события, если оно уже наступило). `ym(...)`-инит
  остался синхронным — очередь `m[i].a` копит вызовы до загрузки скрипта,
  штатный механизм Метрики; `url`/`referrer`/`accurateTrackBounce`
  фиксируются в момент реального захода, не спустя 2 секунды. Компромисс
  сказан владельцу в чате: визиты короче ~2 с не досчитываются.
- [x] Э3-2. Владелец подтвердил: Вебвизором не пользуется. `webvisor: true`
  → `false`.
- [ ] Э3-3. Не сделано — `dns-prefetch` при отложенной на 2+ с загрузке даёт
  пренебрежимый эффект (DNS давно бы успел резолвиться сам), решили не
  добавлять лишний тег ради него.

Эффект: TBT −100–150 мс, 2 длинные задачи уходят из окна загрузки.
Файлы: `index.html`.

### Э4. Картинки: правильный размер, WebP, `width`/`height`, отложенный автоплей

- [x] Э4-1. Логотипы пересохранены в WebP под 2× отображаемого размера
  (dana-holdings 585×176→420×126, happy-planet 200×198→170×168, minsk-mir
  171×200→108×126), Pillow q85 method=6 (сравнил с lossless — lossy оказался
  МЕНЬШЕ на всех трёх, не только «проще», взял lossy). PNG удалены,
  WebP-фолбэка на PNG не заводили (как и планировали). Тот же файл на
  `/minsk/one` (`ObjectLandingPage.tsx`) переведён на `.webp` тоже.
- [x] Э4-2. `/icons/minsk-metro-line3.png` 640×302/72 КиБ → WebP 68×32/2,4 КиБ
  (−97%). Других мест использования не нашлось (grep).
- [x] Э4-3. Hero-фото → WebP q80. Реальный эффект скромнее ожидаемого в
  плане (~15–20%, не ~50%) — источник уже был сжатым JPEG с водяным знаком
  (высокочастотная деталь, плохо жмётся); попробовал q70/q75 — выигрыш
  почти тот же, оставил q80 ради визуального качества hero-картинки (она же
  LCP-кандидат). Оригинальные `.jpg` удалены (нигде больше не используются),
  `og-minsk-mir.jpg` не тронут.
- [x] Э4-4. `width`/`height` добавлены на все три логотипа, иконку метро и
  `HeroImageSlider` (новые пропсы `imageWidth`/`imageHeight` — один размер на
  весь слайдер, все 6 фото одной серии одного размера, не понадобился массив
  `{src,width,height}[]`).
- [x] Э4-5. `loading="lazy"` — только на иконке метро (она реально ниже
  сгиба, в списке видов недвижимости). Логотипы застройщика/УК оставлены
  БЕЗ lazy — они в карточках сразу после hero, граница экрана неочевидна, а
  цена ошибки (задержка показа реально видимого контента) выше пользы от
  1-2 логотипов по 19-20 КБ. Hero — без изменений (`eager`+`fetchPriority=high`).
- [x] Э4-6. Автоплей слайдера (`HeroImageSlider.tsx`) стартует не сразу при
  монтировании, а по `window` `load` + сразу (без дополнительной задержки —
  сам `load` уже достаточно поздний сигнал); плюс предзагрузка следующего
  кадра (`new Image()`) при каждом переключении индекса — переключение не
  становится первым моментом загрузки следующей картинки.

Эффект (по факту): логотипы+иконка −150 КБ, hero-фото −80 КБ, LCP-картинка
легче на ~15%, все атрибуты width/height на месте (CLS-аудит должен стать
зелёным). Файлы: `public/images/district/*`, `public/icons/minsk-metro-line3.*`,
`src/pages/DistrictGuidePage.tsx`, `src/components/objects/HeroImageSlider.tsx`,
`src/pages/ObjectLandingPage.tsx`.

### Э5. Данные первичного рынка: агрегат вместо 6 тысяч строк + починить вторичный рынок (Б2)

- [x] Э5-1. **Б2 исправлен — не так, как было в плане, надёжнее.** Изначальная
  идея (узкий `select` по колоночному GRANT) отброшена: у Postgres
  column-level privileges нет способа "тихо спрятать" непозволенные колонки
  из `select=*`, только упасть целиком — то есть ЛЮБОЙ будущий `select('*')`
  в этом месте (свой или чужой, в новом коде) молча воспроизвёл бы тот же
  баг снова. Вместо этого — SQL-миграция (Management API, реально выполнена
  на проде): анонимный колоночный `GRANT SELECT` на `market_offers` отозван
  ПОЛНОСТЬЮ (таблица теперь закрыта для anon целиком, как большинство
  приватных таблиц), взамен создана вью `public.public_market_offers`
  (`WHERE reviewed=true AND rejected=false`, только 8 безопасных колонок:
  id/deal_type/property_type/size/price_per_sqm/finish_status/has_terrace/
  terrace_area/updated_at) с `GRANT SELECT ... TO anon`. Новая
  `fetchPublicMarketOffers()` в `marketOffersApi.ts` читает эту вью;
  `DistrictGuidePage.tsx` переключён на неё, админский `fetchMarketOffers`
  (`market_offers.select('*')`, `MarketOffersReview.tsx`) не тронут — доступ
  authenticated к самой таблице не менялся. Проверено живым curl'ом anon-
  ключом: `market_offers?select=*` → честный 42501 (было тихим тупиком для
  публичной страницы, теперь явный сигнал "сюда не ходить напрямую"),
  `public_market_offers?select=*` → 394 строки. **Аудит расширен на оба
  конца** — `scripts/audit-rls.mjs`: `market_offers` переехал в `CLOSED`
  (ожидается 0 доступа anon), `public_market_offers` — в `PUBLIC_SELECT_ALL`;
  добавлена ОБЩАЯ проверка `expectSelect && !sel.ok` (раньше аудит проверял
  только "нет доступа там, где не должно быть", но не "есть доступ там, где
  должен" — именно этот пробел и пропустил Б2 в первый раз). Живой прогон
  `node scripts/audit-rls.mjs` — все 48 таблиц + RPC зелёные.
- [ ] Э5-2. **Не сделано — заменено на Э5-4** (см. там). SQL-репликация
  клиентской бизнес-логики (`buildPrimaryMarketPivot`) в отдельную
  Postgres-функцию посчитана более рискованной, чем полезной здесь: рядом с
  ней нет интеграционных тестов, свериться "миллион в миллион" с текущими
  цифрами прода из этой сессии не вышло бы, а разойдись она хоть немного —
  ошибка ушла бы прямо в цифры, которые видит владелец/инвесторы. Узкий
  select + параллельные страницы (Э5-4) убирают почти весь сетевой вес той
  же проблемы намного меньшей кровью.
- [ ] Э5-3. Не сделано — тот же довод об осторожности: после Э5-4 разница в
  весе между "уже загружено на странице" и "догрузить в модалке отдельно"
  стала не так велика (узкий select уже режет байты в разы), а два разных
  места загрузки одних и тех же данных — лишняя развилка кода за небольшую
  выгоду. Если понадобится — отдельным заходом.
- [x] Э5-4. `fetchPrimaryMarketOffers` (`lib/primaryMarketOffersApi.ts`)
  переписан: (а) узкий `select` — 6 полей вместо 13 (`id, category, house,
  area_m2, terrace_area_m2, stage, price_total_eur` — ровно то, что
  использует `buildPrimaryMarketPivot`/`PrimaryMarketProModal`, сверено
  grep'ом по обоим потребителям); (б) сначала один HEAD-запрос
  `{count:'exact', head:true}` узнаёт точное число строк, затем все страницы
  грузятся ПАРАЛЛЕЛЬНО через `Promise.all`, каждая со своим `.order('id')`
  (раньше пагинация шла без ORDER BY — реальный баг корректности, не только
  скорости: Postgres/PostgREST не гарантируют стабильный порядок между
  раздельными запросами без явной сортировки, то есть теоретически были
  возможны пропуски/дубли строк между соседними offset'ами).

Эффект: market_offers — с 0 строк для посетителя (баг) до реальных данных;
primary_market_offers — 7 последовательных запросов по ~40 КиБ (253 КиБ,
6 круговых задержек) → до 7 ПАРАЛЛЕЛЬНЫХ запросов по ~2-3 КиБ (узкий select,
1 круговая задержка на всё). Файлы: `src/lib/primaryMarketOffersApi.ts`,
`src/lib/marketOffersApi.ts`, `src/pages/DistrictGuidePage.tsx`,
`scripts/audit-rls.mjs`, SQL-миграция (view + revoke, выполнена через
Management API).

### Э6. Кэш-заголовки статики (повторные визиты и переходы между страницами)

Vercel по умолчанию отдаёт всё из `dist/` с `max-age=0, must-revalidate` (в
`vercel.json` секции `headers` нет вовсе).

- [x] Э6-1. `vercel.json` получил секцию `headers`: `/assets/(.*)` и
  `/fonts/(.*)` → `public, max-age=31536000, immutable`; `/images/(.*)` и
  `/icons/(.*)` → `public, max-age=2592000` (без immutable). Пререндеренный
  HTML не тронут — рерайты в `vercel.json` идут отдельным блоком, дефолтный
  `max-age=0, must-revalidate` для него остаётся.
- [ ] Э6-2. Проверка `curl -I` на реальных URL прода (`/assets/…js`,
  `/fonts/…woff2`, `/images/district/hero-1.webp`) — ждёт деплоя, из
  песочницы недоступно (проверил только валидность самого JSON и структуру
  правил). Заодно свериться, что `middleware.ts` (404-логика, выполняется
  раньше rewrites) не перетирает эти заголовки — по чтению кода `next()`
  заголовки не трогает, но живая проверка на проде надёжнее.

Эффект: на оценку первого захода почти не влияет, на реальных повторных
визитах — сотни КиБ. Файлы: `vercel.json`.

### Э7. Главный JS-чанк (после Э0–Э6, только по замерам) — НЕ СДЕЛАНО В ЭТОМ ЗАХОДЕ

`index-*.js` был 594 КБ/162 КиБ gz, после Э1-Э4 (шрифты/карты ушли в свои
чанки) — 560 КБ/158 КиБ gz (перепроверено `npx vite build` после всех правок
этой сессии): все публичные страницы (`ObjectLandingPage`,
`BusinessCentersMinskPage`, `BusinessCenterDetailPage`, `MinskHub`,
`PublicBuildingPlan`, `BriefPublicPage`, `MeetingSummaryPublicPage`, сам гид)
в `App.tsx` по-прежнему импортированы статически — посетитель гида скачивает
код бронирования кабинетов и каталога БЦ. Комментарий в `App.tsx` объясняет
это «нельзя лишний сетевой перелёт для публичных страниц» — это было верно ДО
пререндера; с пререндером (Э0) контент виден до JS, и один дополнительный
чанк уже не критичен. **Сознательно отложено на следующий заход** — это
самая рискованная и самая объёмная правка из всего плана (переезд на
`lazy()` нескольких публичных страниц сразу требует правки `App.tsx` и
пререндер-снапшотов каждой из них, чтобы не создать тот самый «лишний
сетевой перелёт», от которого изначально и защищались), а первые 6 этапов
уже дают основной прирост баллов. Начинать с Э7-1 (замер) на свежих цифрах
после всего остального.

- [ ] Э7-1. Сначала замерить: `npx vite build` + `npx vite-bundle-visualizer`
  (или `rollup-plugin-visualizer`) — что именно занимает главный чанк.
  Заранее известно (grep по собранному чанку): планировки/бронирование
  (`BuildingPlanCanvas` уже отдельный чанк, но `buildingPlans*`, `objectsApi`,
  `imageCompress`, `telegramAvatarApi` — в `modulepreload` шелла, т.е. грузятся
  на КАЖДОЙ странице), рабочие места (`workstation`), `marked`.
- [ ] Э7-2. Перевести на `lazy()` те публичные страницы, которые не
  открываются с гида по ссылке первым переходом — как минимум
  `PublicBuildingPlan`, `BriefPublicPage`, `MeetingSummaryPublicPage`
  (токен-страницы, у них свои html-шеллы и noindex, пререндера нет, но и
  трафика из поиска нет). `ObjectLandingPage`/`BusinessCenters*`/`MinskHub`
  — решать по цифрам Э7-1; если переводить — добавить `<link rel="modulepreload">`
  на их чанк в пререндер-снапшот соответствующей страницы (иначе тот самый
  лишний перелёт).
- [ ] Э7-3. `supabase-*.js` (53 КиБ gz, 43 неиспользуемых): публичным
  страницам нужен только PostgREST `GET` — можно заменить на тонкий
  `fetch`-клиент (как в `scripts/prerender.mjs`), но это правка всех
  `*Api.ts`, которые используют публичные страницы. Только если Э7-1
  покажет, что это последнее крупное место. Отдельно проверить, что
  `@supabase/supabase-js` не тянет `realtime`/`auth` в главный чанк без нужды
  (флаг `createClient(..., { realtime: … })` / отдельный клиент для публички).

Эффект: −60–100 КиБ gz на первой загрузке, TBT −50–100 мс. Файлы: `src/App.tsx`,
`src/lib/supabase.ts`, `*Api.ts`.

### Э8. Accessibility (контраст) — параллельно с любым этапом, отдельный коммит

Все 86 → 100 упираются только в контраст (см. п.0.9). Это дизайн-решения,
менять цвета всему сайту разом — не по этому плану. Минимально:

- [x] Э8-1. `--color-ink-faint` #9a9ba3 → `text-ink-muted` (#6b6d76, 5,15:1
  на белом) везде, где текст смысловой: в `DistrictQuarterMap.tsx` — обе
  подписи селекторов, легенда «Меньше/Больше», все 3 подписи-факта под
  картой; в `DistrictGuidePage.tsx` — 21 инстанс (табличные заголовки,
  цены в ячейках, подписи под таблицами, eyebrow-labels типа «Содержание
  гайда»/«Плюсы»/«Минусы»/«В самом районе», группы организаций и т.п.).
  Оставлены `text-ink-faint`: декоративные разделители «·», плейсхолдер «—»
  для отсутствующего значения, «адрес не присвоен» (редкий fallback), и все
  транзитные состояния («Загрузка карты…», «Загрузка…», «Данные пока не
  собраны.» — momentary UI, не основной контент).
- [x] Э8-2. «RED» в логотипе шапки (2 инстанса — обычный и sticky-дубль):
  `text-primary` → `text-primary-hover` (#c81124, 5,13:1 на `bg-bg`). Ссылку
  «деловой центр Red One» НЕ трогал — в отчёте Lighthouse была явно названа
  только «RED» (`<span class="font-black text-primary">`), а не эта ссылка;
  не стал гадать за scope отчёта.
- [x] Э8-3. Бейдж «Обновлено: …» и обе цифры дефицита офисов
  («N предложение на продажу»/«N в аренду», тот же паттерн `text-success` на
  `bg-success-bg`, найден при проверке — не был в исходном отчёте, но та же
  пара цветов и тот же провал контраста) — `text-success` → произвольный
  Tailwind-класс `text-[#0f6b3d]` (5,88:1 на `bg-success-bg`), не глобальный
  токен `--color-success` (тот используется по всему приложению).

Проверено программно (Python, формула контраста WCAG) для всех финальных
цветовых пар — не на глаз. Файлы: `src/pages/DistrictGuidePage.tsx`,
`src/components/district/DistrictQuarterMap.tsx`. Глобальные токены
`index.css` не трогали.

## 3. Что НЕ делаем (и почему)

- **Не переезжаем на SSR/Next.js.** Пререндер (Э0) + клиентский рендер
  закрывает те же метрики для контентных страниц; переезд стека — недели.
- **Не убираем Яндекс.Карты и Метрику.** Только откладываем их загрузку
  (Э2/Э3) — функционал и статистика остаются.
- **Не меняем Montserrat на системный шрифт** — фирменный стиль; экономим
  форматом и сабсетом (Э1).
- **Не подключаем image-CDN/сервис ресайза** — картинок на странице
  ~10, все статичные, достаточно один раз пересохранить (Э4).
- **Не делаем Service Worker/офлайн-кэш** — избыточно для сайта-визитки,
  риск залипания старых версий после деплоя.
- **Не трогаем глобальные токены цвета в `index.css`** ради контраста
  (Э8) — это затронет всю админку; правим точечно на публичной странице.
- **Не оптимизируем `/admin/*`** — за паролем, PageSpeed туда не ходит.

## 4. Как проверять (в песочнице и на проде)

- Сборка: `npx tsc -b && npx vite build` — обязательно перед каждым
  коммитом. Полный `npm run build` в песочнице упирается в `prerender.mjs`
  (виснет на сети до Supabase — известно, см. журнал CLAUDE.md) — сам
  пререндер проверяется только на проде (Э0-5).
- Размеры чанков: `ls -S dist/assets/*.js | head` + `gzip -c … | wc -c`.
- Локальный Lighthouse (1st-party часть): `npx vite preview --port 4173` и
  `CHROME_PATH=/opt/pw-browsers/chromium npx lighthouse
  http://localhost:4173/minsk/minsk-mir --only-categories=performance
  --form-factor=mobile --chrome-flags="--headless --no-sandbox"
  --output=json --output-path=./scratchpad/lh.json`. Внешние домены
  (yandex, supabase) из headless-браузера песочницы заблокированы — цифры
  будут лучше прода, годятся только для сравнения «до/после» по 1st-party
  (шрифты, картинки, чанки), не как итоговая оценка.
- Итоговая оценка — только PageSpeed Insights на проде, владелец после
  каждого деплоя (мобильный + десктоп). Смотреть не на балл, а на
  FCP/LCP/TBT и список «Insights» — балл шумит на ±5 между прогонами.
- Визуальная проверка (карты, слайдер, таблицы) — мок-тест Playwright по
  обычному правилу проекта (временный роут, скриншот, откат).

## 5. Вопросы/действия для владельца (блокеры)

- ~~**В1** (для Э0-1) — Build Logs.~~ **Прислано, использовано** — точный
  диагноз см. Б1/Э0.
- ~~**В2** (для Э3-2) — Вебвизор.~~ **Ответ: не нужен** — `webvisor: false`.
- **В3 (для Э5-1)** — не задавался повторно, решение принято по факту:
  публичная страница переведена на отдельную вью `public_market_offers`,
  админский путь не тронут. Другие потребители `select('*')` на публичных
  таблицах не проверялись заново в этом заходе — если решите продолжать эту
  линию, стоит повторить grep по `select('*')` во всех `*Api.ts`, которые
  дёргают публичные страницы.
- **В4** — реализовано **как правильнее** (владелец, дословно): каждый
  этап — отдельный логический блок правок в этой же сессии на ветке
  `claude/pagespeed-analytics-optimization-1rcvip`; решение по мерджу в
  `claude/redevelopment-platform-prototype-oodobu` (прод) — см. запись в §6
  ниже, финальный коммит этой сессии ещё не смёржен, ждёт явного запроса.
- **Новый В5** — Э2-4 (сокращение набора модулей Яндекс.Карт через `load=`)
  не сделан: изменение поведения стороннего API нельзя проверить из этой
  песочницы (заблокирован прокси), а ошибиться — значит сломать обе карты
  на живом сайте без возможности откатить быстро. Если хотите это сделать —
  нужен отдельный заход с проверкой прямо на проде (задеплоить, сразу же
  руками открыть обе карты и полноэкранный режим).
- **Новый В6** — Э7 (главный JS-чанк) сознательно отложен на следующий заход
  — самая объёмная и рискованная правка из всего плана, а первые 6 этапов
  уже должны дать основной прирост баллов. Если после повторного прогона
  PageSpeed захотите продолжать — начинать с Э7-1 (замер актуального
  состояния бандла).

## 6. Журнал выполнения

- **2026-09-06 — реализация Э0–Э6, Э8 (без Э7, без Э2-4); Б1 и Б2 исправлены.**
  Владелец прислал Build Logs (В1) и ответ по Вебвизору (В2, не нужен), на
  вопрос про порядок мерджа ответил «как правильнее, так и делай» — все
  этапы сделаны и закоммичены на ветке этой сессии одним заходом, без
  промежуточных мерджей в прод (решение по мерджу — см. §5, В4).
  - **Э0** — реальная причина Б1 (см. там) подтверждена логами: `--single-process`
    у `@sparticuz/chromium` на Vercel. `WORKER_COUNT` 4→1 (строго
    последовательный рендер), 3 попытки с бэкоффом, критичные пути
    (лендинги+статика) валят сборку явным `process.exitCode=1` при провале,
    `loadYmaps()` не грузится в режиме `?prerender=1`. Проверка на самом
    проде (Э0-5) не сделана — ждёт деплоя.
  - **Э1** — TTF→WOFF2 (pyftsubset), 1,5 МБ→100 КБ, 3 preload. По ходу найден
    и исправлен собственный баг: путь-глоб в CSS-комментарии обрывал сам
    комментарий (последовательность «звёздочка перед слэшем» — закрывающая
    для `/* … */`), из-за чего минификатор терял `@font-face` для Regular;
    поймано прогоном `vite build` + `grep` по собранному CSS, не на глаз.
  - **Э2** — `useInView.ts` (новый общий хук), обе карты (компактная версия)
    грузятся только при приближении к вьюпорту; `DistrictMap` переведён на
    `lazy()`, как и `DistrictQuarterMap`. Э2-4 не сделан (см. §5, В5).
  - **Э3** — `tag.js` вставляется в DOM с задержкой (после `load`+2с),
    `webvisor: false`.
  - **Э4** — все картинки страницы (3 логотипа, иконка метро, 6 hero-фото) в
    WebP, `width`/`height` везде, автоплей слайдера ждёт `load` + предзагрузка
    следующего кадра.
  - **Э5** — Б2 исправлен вью `public_market_offers` (не узким select'ом,
    как планировалось изначально — Postgres column-privileges не позволяют
    частично разрешить `select=*`, см. Э5-1) + отзыв прямого GRANT anon на
    `market_offers`; `audit-rls.mjs` дополнен проверкой «ожидался разрешённым,
    но получил отказ» (пробел, из-за которого Б2 не поймали раньше). Данные
    первичного рынка — узкий select + параллельная загрузка страниц вместо
    последовательной, `.order('id')` (Э5-4); Э5-2/Э5-3 (агрегат в БД,
    ленивая загрузка Pro-модалки) сознательно не делались — риск разойтись
    с бизнес-логикой без интеграционных тестов посчитан выше, чем польза.
  - **Э6** — кэш-заголовки в `vercel.json` (`immutable` для assets/fonts,
    30 дней для images/icons).
  - **Э8** — контраст: 21 инстанс `text-ink-faint`→`text-ink-muted` в
    `DistrictGuidePage.tsx` + вся `DistrictQuarterMap.tsx`, «RED» в шапке →
    `text-primary-hover`, бейдж «Обновлено»/дефицит офисов →
    `text-[#0f6b3d]`. Все финальные пары контраста проверены программно
    (Python, формула WCAG), не на глаз.
  - **Не сделано**: Э0-5/Э6-2 (проверка на живом проде — ждёт деплоя),
    Э2-4, Э5-2/Э5-3, Э7 целиком (см. §5 про В5/В6).
  - **Проверено в песочнице**: `npx tsc -b` и `npx vite build` чистые на
    каждом шаге; `node scripts/audit-rls.mjs` — все 48 таблиц + RPC зелёные
    против реальной базы; SQL-миграция (view+revoke) выполнена и проверена
    живым curl'ом anon-ключом. Полный `npm run build` (с `prerender.mjs`) в
    песочнице не прогнать до конца — headless-браузер здесь не может дотянуться
    до `*.supabase.co` (подтверждено отдельным probe-скриптом: запрос висит
    на уровне соединения, ни ответа, ни ошибки), поэтому сам факт починки Б1
    на реальном Vercel-контейнере проверяется только после деплоя (Э0-5).
